1. ?.
1-1. 앞의 평가 대상이 undefined거나 null이면 평가를 멈추고 undefined를 반환한다.
1-2. 객체에 사용하는 특별한 문법 구조체이다. => 연산자가 아니다.
1-3. ex) Object?.assign()
=> Object라는 객체가 존재하면 Object.assign을 호출하라


2. ??
2-1. ??는 연산자이다.
2-2. ex) a ?? b
=> a가 null도 아니고 undefined도 아니면 a 선택
=> 그 외의 경우는 b를 선택
2-3. 값이 할당된 변수를 찾는데 사용한다.


