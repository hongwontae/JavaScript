JS에서 데이터가 변경되었을 떄 원시값은 불변성을 보장하고 객체값은 불변성을 보장하지 않는다.

만약 객체값이나 배열값을 복사하는 메서드를 사용해서 새로운 참조값 주소를 갖는 변수를 만들었더라도
중첩 객체나 중첩 배열은 기존의 주소를 갖고 있다.

Object.assign() => 중첩 객체의 값은 동일한 주소값을 갖는다.
스프레드 연산자를 통해 복사도 중첩 객체나 중첩 배열은 동일한 참조값을 갖는다.

스프레드 연산자와 화살표 함수를 같이 사용하면 중첩 배열이나 중첩 객체도 새로운 주소값을 갖는다.