        // 기본 타입은 5개
        // Number, Boolean, String, Null, Undefined
        //Nan => 엄밀히 따져서 js에서 숫자타입이다.

        // == 이중 등호는 서로 같음을 물어본다.
        // 타입이 다르면 강제로 형 변환을 해 알려준다.

        // 삼중 등호는 타입을 구분하고 값과 타입 모두를 비교한다.
        // 즉 강제형변환을 하지 않는다.

        // alert => 브라우저의 클라이언트에게 내용을 알려준다.
        // prompt => 브라우저의 클라이언트에게 값을 받아와서 String으로 받아온다.
        // parseInt => 스트링을 정수로 변환한다.
        console.log("hello from our from")
        console.log(1+3)
        let total = 1+3;
        let cc = "123"
        console.log(parseInt(cc))
        