DevStory 3 Template Literal

백틱을 사용한다. ``;

1. 백틱안에 작은 따옴표나 큰 따옴표는 문자로 인식된다.
2. 문자열로써 백틱을 사용하고 싶다면 \(백 슬래시)를 앞에 두면 된다.
3. ${} => 변수 표현식을 삽입할 수 있다. 이러한 방법을 보간 표현식이라고 한다.
4. 작은 따옴표나 큰 따옴표는 js 코드 내에서 행 바꿈이 불가능하다. 템플릿 리터럴은 가능하다.
5. '',"" => /n을 써야 개행이 된다. 템플릿 리터럴은 그냥 코드 상에서 enter를 통해 행 바꿈을 하면 된다.
6. 템플릿 리터럴 안에서 삼항 연산자를 통해 동적으로 표현할 수 있다.
