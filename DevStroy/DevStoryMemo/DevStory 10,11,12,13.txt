10. JS의 배열 내부에서 특정 값의 개수를 구하는 방법

for, map, filter를 사용한다.


JS의 array의 size(), length
=> new Array()로 배열을 만들고 해당 배열이 문자열로만 이루어져있다면 arr.length의 값은 항상 0이다.
=> lastKeyValue+1를 반환하기 때문이다.


11. 널 병합 연산자

?
=> 왼쪽 피연산자가 null 또는 undefined일 경우 오른쪽 피연산자를 반환하고 그렇지 않을 경우 왼쪽 피연산자를 반환한다.
=> ||, &&보다 연산순위가 낮다.
=> 객체의 정의되지 않은 속성에 접근할 경우 사용한다.

12. 콜백 함수
=> 함수의 인자나 매개변수로 다른 함수가 전달되는 함수을 의미한다.


13. 배열에서 중복값을 제거하는 방법

13-1. Set객체와 spread 연산자를 사용한다.
13-2. filter 함수와 indexOf메서드를 사용한다.
13-3. filter 함수와 hasOwnProperty를 사용한다.

hasOwnProperty 함수의 특징
=> 숫자와 문자열을 구분하지 않는다.
=> 프로퍼티 값이 아닌 프로퍼티 이름을 확인한다.



















