const ul = document.querySelector('ul');
const li = document.createElement('li');

const a1 = ul.children[1]
li.textContent = 'like that'
const a2 = a1.before(li) // 전

const a3 = a1.after(li) // 후









